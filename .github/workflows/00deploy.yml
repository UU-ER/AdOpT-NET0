name: Deploy

on:
  pull_request:
    branches:
      - 'main'

jobs:
  autoformatting:
    uses: ./.github/workflows/autoformatting.yml
  testing:
    needs: autoformatting
    uses: ./.github/workflows/testing.yml

