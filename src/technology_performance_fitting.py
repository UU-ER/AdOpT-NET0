# import statsmodels.api as sm
# import numpy as np

def fit_linear_performance(dim_in, dim_out, x_points, y_points, through_origin, tec_type):
    # if tec_type == 1:
    #     alpha = dict()
    #     for car in x_points.keys():
    #         x = x_points[car]
    #         y = y_points[car]
    #         linmodel = fitlinearcurve(x,y,through_origin)
    #         alpha[car] = slope
    alpha = 1
    return alpha